<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>tech.kayys.wayang</groupId>
        <artifactId>wayang</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>wayang-runtime-standalone</artifactId>

    <properties>
        <compiler-plugin.version>3.14.1</compiler-plugin.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.version>3.31.1</quarkus.platform.version>
        <skipITs>true</skipITs>
        <surefire-plugin.version>3.5.4</surefire-plugin.version>
        <gamelan.version>${project.version}</gamelan.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Wayang Modules -->
        <dependency>
            <groupId>tech.kayys.wayang</groupId>
            <artifactId>wayang-control-plane-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>tech.kayys.wayang</groupId>
            <artifactId>wayang-plugin-registry</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>tech.kayys.wayang</groupId>
            <artifactId>wayang-plugin-spi</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>tech.kayys.wayang</groupId>
            <artifactId>wayang-orchestration-engine</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>tech.kayys.wayang</groupId>
            <artifactId>wayang-vault-manager</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Quarkus Core -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-jackson</artifactId>
        </dependency>

        <!-- Database & Persistence (Standalone = H2 + Hibernate Reactive) -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-reactive-panache</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-reactive-pg-client</artifactId>
            <!-- Note: Control Plane uses reactive-pg-client by default if configured so in its
            persistence.xml/domain classes
                 However, standalone architecture says H2/SQLite. 
                 Hibernate Reactive supports PostgreSQL, MySQL, DB2, MSSQL. It does NOT fully support H2 reactive in
            the same way.
                 BUT, standard Hibernate ORM + JDBC supports H2. 
                 
                 If ControlPlaneCore connects to DB via Panache Reactive, we must use a Reactive driver.
                 Postgres is standard. For standalone, we might need a docker container or testcontainers if we
            stick to Reactive.
                 OR we switch to H2 IF Vertex/Hibernate Reactive supports it now, or we switch to standard generic
            JDBC if we refactor.
                 
                 Wait, Wayang architecture doc said: "Embedded DB (H2/SQLite)" for standalone.
                 But WayangProject extends PanacheEntityBase (Reactive).
                 Hibernate Reactive DOES NOT support H2 well (it's experimental or limited).
                 
                 Decision: For "glue all", I will assume we might need to run a small Postgres container OR try H2
            if feasible.
                 Actually, simpler: use H2 with standard Hibernate Panache if we could, but the code I wrote uses
            Reactive.
                 
                 Let's stick to what's capable. IF the user wants standalone without external DB, we usually use H2.
                 If Hibernate Reactive is strict, maybe we use Postgres. 
                 BUT, "Standalone" implies self-contained. 
                 
                 Let's check if we can use H2 with Reactive. As of late 2024 (projected from 2026 :)), maybe it
            works?
                 Actually, let's look at `wayang-control-plane-core` usage. It explicitly imports
            `io.quarkus.hibernate.reactive.panache.PanacheEntityBase`.
                 
                 If I am forced to use reactive, I'll stick to Postgres driver but maybe configure it to use a local
            or embedded postgres if possible, or just add the driver. 
                 Integrating H2 reactive driver: `quarkus-reactive-db2`? No.
                 `quarkus-reactive-mysql`? No.
                 
                 There IS `quarkus-reactive-h2` extension in recent Quarkus versions for testing mostly, but let's
            try.
                 Actually, let's stick to the Postgres driver as a placeholder for "DB" and I'll verify if strict
            standalone (no-docker) is a hard requirement that breaks this.
                 
                 Wait, `wayang-control-plane-core` uses `quarkus-reactive-pg-client` in its pom. 
                 So I will include `quarkus-reactive-pg-client`. If the user wants true embedded, we might need
            `embedded-postgres`.
            -->
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                            <goal>native-image-agent</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler-plugin.version}</version>
                <configuration>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <configuration>
                    <argLine>--add-opens java.base/java.lang=ALL-UNNAMED</argLine>
                    <systemPropertyVariables>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <argLine>--add-opens java.base/java.lang=ALL-UNNAMED</argLine>
                    <systemPropertyVariables>
                        <native.image.path>
                            ${project.build.directory}/${project.build.finalName}-runner</native.image.path>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>native</id>
            <activation>
                <property>
                    <name>native</name>
                </property>
            </activation>
            <properties>
                <quarkus.package.jar.enabled>false</quarkus.package.jar.enabled>
                <skipITs>false</skipITs>
                <quarkus.native.enabled>true</quarkus.native.enabled>
            </properties>
        </profile>
    </profiles>
</project>