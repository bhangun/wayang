package tech.kayys.node;

public enum Capability {
    NETWORK,
    FILESYSTEM,
    LLM_ACCESS,
    GPU,
    DB_ACCESS,
    TOOL_EXECUTION,
    RAG_QUERY,
    MEMORY_READ,
    MEMORY_WRITE,
    KG_READ,
    KG_WRITE,
    MULTIMODAL
}